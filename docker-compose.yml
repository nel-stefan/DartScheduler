services:
  dartscheduler:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - dart_data:/data
    environment:
      - DATABASE_PATH=/data/dartscheduler.db
      - DATABASE_TYPE=sqlite
      - PORT=8080
      # - CLUB_NAME=DARTCLUB GROLZICHT
      # - APP_TITLE=DartScheduler
      # - LOGO_PATH=/data/logo.png
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  dart_data:
